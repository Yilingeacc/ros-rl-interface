# Message for returning the current sensation vector
# a scalar reward signal from the environment
# and a flag which represent whether the environment has terminated
# Note. only for nav_msgs/Odometry

nav_msgs/Odometry state
float32 reward
bool done
